# Production Environment Configuration for Pulse Backend
# Copy this file to .env and fill in your actual values

# Node Environment
NODE_ENV=production
PORT=3000

# Database Configuration
POSTGRES_USER=pulse_user
POSTGRES_PASSWORD=change_this_to_a_strong_password
POSTGRES_DB=pulse_db

# Database URL (automatically constructed in docker-compose.production.yml)
# DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?schema=public

# WorkOS Authentication
# Get these from https://dashboard.workos.com
WORKOS_CLIENT_ID=your_workos_client_id_here
WORKOS_API_KEY=your_workos_api_key_here
WORKOS_COOKIE_PASSWORD=change_this_to_32_random_characters

# URLs (IMPORTANT: Update with your actual domain)
# These are used for OAuth redirects and API callbacks
BACKEND_URL=https://api.yourdomain.com
FRONTEND_URL=https://yourdomain.com

# CORS Configuration (optional)
# Comma-separated list of allowed origins for production
# If not set, defaults to FRONTEND_URL and localhost
# Example: CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com,https://yourapp.vercel.app
CORS_ORIGINS=https://yourdomain.com
